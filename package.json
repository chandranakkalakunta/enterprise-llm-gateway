{
  "name": "enterprise-llm-gateway",
  "private": true,
  "packageManager": "pnpm@11.5.2",
  "engines": {
    "node": ">=22 <23",
    "pnpm": ">=10"
  },
  "scripts": {
    "dev": "pnpm --filter @ellmgw/gateway dev",
    "start": "pnpm --filter @ellmgw/gateway start",
    "test": "pnpm --filter @ellmgw/gateway test",
    "lint": "pnpm --filter @ellmgw/gateway lint",
    "format": "pnpm --filter @ellmgw/gateway format",
    "typecheck": "pnpm --filter @ellmgw/gateway typecheck",
    "build": "pnpm --filter @ellmgw/gateway build"
  }
}
