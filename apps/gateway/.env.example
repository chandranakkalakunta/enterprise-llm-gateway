NODE_ENV=development
PORT=8080

# Google OIDC (1.3) — placeholders only. Never commit real values.
OIDC_ISSUER=https://accounts.google.com
OIDC_CLIENT_ID=
OIDC_CLIENT_SECRET=
OIDC_AUDIENCE=
OIDC_REDIRECT_URI=http://localhost:8080/auth/callback
ADMIN_EMAILS=admin@chandraailabs.com

# Phase 1.2 (comments only — do not put secret values here)
# GCP_PROJECT_ID=ellmgw-dev
# GCP_REGION=asia-south1
# Secret Manager placeholders (set versions out of band):
#   ellmgw-gateway-grok-api-key
#   ellmgw-gateway-oidc-client-secret
