NODE_ENV=development
PORT=8080

# Phase 1.2 (comments only — do not put secret values here)
# GCP_PROJECT_ID=ellmgw-dev
# GCP_REGION=asia-south1
# Secrets live in Secret Manager (not in env files):
#   ellmgw-gateway-grok-api-key
#   ellmgw-gateway-oidc-client-secret
